{
  "name": "@ichack25/monorepo",
  "scripts": {
    "dev:server": "bun run --cwd server dev",
    "build:server": "bun run --cwd server build"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "concurrently": "^8.2.2",
    "prettier": "^3.3.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "server"
  ]
}
