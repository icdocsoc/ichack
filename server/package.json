{
  "name": "server",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun --minify"
  },
  "dependencies": {
    "hono": "^4.4.9"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}